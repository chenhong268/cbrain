# CBrain — Agent 操作协议

你是 CBrain 的使用者。CBrain 是你的持久化知识大脑，通过 CLI 或 MCP 连接。跨对话记住一切。

## 核心原则：CBrain First

任何知识性问题，第一步查 CBrain。你不知道用户在大脑里存了什么——先查，再答。

```
CBrain → 自有记忆 → 网上搜索
```

## 路由：先看 RESOLVER.md

收到请求后，先对照 `skills/RESOLVER.md` 判断该用哪个 skill：

| 用户说什么 | 路由到 |
|-----------|--------|
| 总结/梳理/复盘/全面了解/什么来头 | `skills/review.md` |
| 什么关系/怎么认识的/A和B什么联系 | `skills/connect.md` |
| 清理大脑/去重/有什么该删的 | `skills/cleanup.md` |
| 帮我写/写个周报/写段介绍/朋友圈 | `skills/write.md` |

匹配到 skill 后，严格按 skill 的协议执行。没匹配到就用 `skills/query.md` 做普通搜索。

## 四条铁律

### 1. 深度复盘（review）

禁止 query 一次没结果就上网搜。强制走 5 步：
1. query 关键词+别名+中英文
2. get_page 拉取相关页面全文
3. graph_query traverse + backlinks
4. get_timeline
5. 合成输出，每条标注来源

5 步全做了仍然零信息，才上网搜。

### 2. 关联分析（connect）

1. resolve_slugs 确认双方存在
2. graph_query 双向找最短路径
3. 交叉比对找共同关联
4. get_timeline 查时间线交集
5. 合成：直接关系→中间人→共同关联→时间线→总结

### 3. 清理（cleanup）

先列出问题，等用户确认后再执行。禁止跳过确认直接删除。
1. health 扫描
2. 列出：编号、名称、建议操作
3. 问用户怎么处理
4. 只处理确认的项
5. sync 收尾

### 4. 写作（write）

1. 先确认受众、长度、语气
2. 从 CBrain 搜素材
3. 列出缺口
4. 只写 CBrain 里有的事实，不编
5. 输出后问要不要存档

成文不需要 [Source:...] 引用——那是 review 用的。

## 重要规则

- **raw/ 只读**，brain/ 可写。不要修改 raw/ 下的文件
- **存新信息用 ingest**，原文整篇传入，不拆分
- **每条事实有出处**，不确定的宁可不写
- **所有 ingest 进 brain/** ，不要进 raw/

## 开发治理协议

CBrain 的开发采用“用户定方向、Hermes 反馈体验、Claude Code 实现、Codex 总控审核”的协作模式。目标不是堆功能，而是让 CBrain 逐步成为可靠、可解释、可治理的 Agentic Memory / Research Kernel。

### 角色职责

- **用户**：决定产品愿景、隐私边界和重大架构取舍；对需要长期承诺的方向最终拍板。
- **Hermes Agent**：作为日常使用者反馈真实调用痛点，包括成功率、相关性、上下文消耗、噪声和工作流摩擦。
- **Claude Code**：按已确认的 issue 边界实现代码与测试；不得自行扩张范围或顺手进行无关重构。
- **Codex**：承担 PM + CTO + reviewer + release manager 职责，负责需求判断、路线排序、issue 拆解、架构把关、commit review、集成判断、发布门禁和上线后复盘。

### 标准开发流程

1. 用户或 Hermes 提出实际痛点、目标或运行反馈。
2. Codex 先检查最新代码与已有 issue，区分 bug、体验问题、架构问题和战略能力，给出优先级及方案。
3. 涉及方向、公开 API、数据模型、隐私或重大复杂度取舍时，Codex 与用户讨论并达成共识后再创建或调整 issue。
4. Codex 将工作拆成小而可验收、可回滚的 issue，写清范围、非目标、测试要求、依赖顺序和验收标准。
5. Claude Code 每次只实现当前 issue，并提交独立 commit。
6. Codex 审核 commit，优先报告 bug、回归风险、架构偏移、数据一致性问题和缺失测试，并明确是否接受、退回修正或调整后续路线。
7. 通过审核的变更进入集成与 release gate：检查测试、迁移、兼容性、回滚方式、release note 和真实 Agent 使用影响。
8. 发布后基于 Hermes 反馈、health 状态、搜索 trace 和运行日志做效果复盘，形成下一轮优先级。

### 架构守则

- **确定性执行优先**：LLM 可用于规划、判断与综合，事实写入、索引更新、关系约束、去重和审计应由可验证代码执行。
- **Ontology 是规则源头**：实体类型、关系类型、结构化字段及其约束应优先从 ontology 获取，避免分散硬编码。
- **证据可追溯**：检索、推理和最终输出应能追溯到 page、chunk、link、timeline 或其他明确来源。
- **状态一致**：Markdown vault、SQLite、FTS 与向量索引之间的一致性优先于新增智能能力。
- **分层路由**：简单查询保持快速稳定；只有确实需要关系分析、时间线、多实体比较或充分性判断的查询才进入 agentic 流程。
- **可观测且有预算**：agentic 行为必须可记录、可解释，并受调用次数、延迟和上下文预算约束。

### 审核与决策门

Codex 在审核 Claude Code 提交时，不只检查局部实现，还必须检查目标正确性、架构一致性、数据可靠性、Agent 使用体验、演进成本和测试质量。

出现以下情况时，必须暂停默认推进并与用户讨论达成共识：

- agentic 能力明显牺牲简单查询的速度、稳定性或可预测性；
- LLM 获得过度自由执行权，导致写入或回答无法审计；
- 新模块与现有搜索、研究或数据治理能力重复建设；
- 单一 issue 需要引入大范围重构或公开接口破坏性变化；
- 数据模型、知识治理策略、迁移方案或长期运行成本发生重大变化；
- 涉及隐私边界、事实来源或置信度原则的取舍。

### 永久反过度工程化门禁

该门禁是人工决策合同，不是复杂度评分、脚本或 CI 拦截器。测试全绿是必要条件，但不能替代范围与复杂度判断。

#### 实现前：证据与最小方案

每个 issue 必须先说明：

1. 已复现的用户问题、运行事故、测量瓶颈或合同缺口是什么；
2. 不改会造成什么实际后果；
3. 能解决证据的最小局部修改是什么；
4. 为什么现在需要更大的方案，哪些部分可以等第二个真实用例再做。

路线愿景本身不是实现证据。探索性工作的首个交付物应是最小测量或原型，不能静默演变为生产框架。

出现以下任一情况时，暂停默认实现，由 Codex 给出 `approve`、`simplify`、`split` 或 `reject` 建议，并由用户批准后继续：

- 新增 MCP tool/action、CLI command/flag 或 HTTP endpoint；
- 新增数据库表、迁移、持久状态族、后台循环或进程；
- 改变公开默认值、隐私边界或兼容性合同；
- 单一 issue 修改超过三个独立子系统；
- 预计新增超过 300 行生产代码；
- 为单一现有生产调用方引入 registry、plugin layer、通用 framework 或抽象层。

这些条件只触发判断，不自动否决，也不得通过拆文件或转移代码规避。隐私、数据一致性、回滚、fail-closed 和确定性恢复所需的复杂度可以保留，但必须对应已复现风险，并尽量隔离于常规热路径。

#### 实现中：范围与消费者

- 一个 issue 通常只包含一个用户可观察行为、一个连贯状态转换和一个可独立回滚交付；无关清理不得顺手带入。
- 可复用抽象必须有至少两个已存在、可点名且行为实质相同的生产消费者；测试、路线图和假想调用方不算。只有一个消费者时优先局部函数或窄模块。
- 新 alias、兼容分支、feature flag 或临时 fallback 必须写明存在原因、无隐私的使用观测方式、最早移除版本或条件，以及负责复审/删除的 issue。无法观测时按永久复杂度审核。
- 不为降低行数做独立的大文件重写；只在已批准 issue 确实触及相应职责时渐进拆分。
- AST/source-shape 检查、mutation matrix 和 frozen evidence chain 必须对应明确威胁模型，不扩张到任意混淆或假想攻击者。
- host-specific proof 只进入 release-only 路径；没有新证据时，不复制到常规服务或默认开发流程。

#### 交付前：做减法审核

Codex 的审核结论必须回答：

- 删除或避免了哪些代码、接口或行为；
- 每个新抽象是否确有两个真实消费者；
- 测试是在保护外部行为，还是冻结实现细节；
- 是否新增当前用户流程不需要的接口、状态族或兼容路径；
- 产品收益是否值得持续维护与发布成本。

#### 流程复杂度预算

- 只读检查、状态报告和小型文档修正不制造实现型文档。
- 已批准的 issue 或 spec 不重复开放式探索，除非新证据推翻既有决定。
- issue 已有可执行合同的小型、局部、可回滚修改，不机械重复创建 spec 与长计划。
- 只有命中上述复杂度触发器，或涉及重大数据、隐私、兼容风险时，才升级完整设计与规划流程。
- Skill 和审核流程用于提高质量，不得扩张需求或制造重复交付物；如上级执行环境强制要求流程产物，使用满足要求的最小版本。

### 隐私约束

- 公开 issue、PR 描述、测试示例、设计文档和演示数据中，不得写入真实人名、公司名、组织名、产品名、具体物品名或其他可能识别用户知识库内容的信息。
- 需要举例时统一使用匿名占位符，例如“实体A”“实体B”“组织C”“主题D”。
- 审核提交时应检查新增测试、日志样例和文档是否意外泄露真实知识内容。
