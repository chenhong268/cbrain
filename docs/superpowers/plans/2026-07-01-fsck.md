# fsck 只读一致性检查器 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增只读 `cbrain fsck` CLI，跨 vault/SQLite/FTS/LanceDB 四层报告存储一致性，不写数据，输出 human + 稳定 JSON，建议修复命令不执行。

**Architecture:** 分层 probe——`src/core/fsck/` 下 vault/sqlite/fts/lance 四个 probe 独立探测，`report.ts` 聚合，`src/cli/commands/fsck.ts` 编排输出 + exit code。复用 `db.checkFkViolations()`（只读 FK 孤儿）和 `LanceDBManager.openChunksStrict()`（不建表）。severity 4 级（critical/error/warning/info），overallStatus pass/warn/fail，exit 0/1/2。

**Tech Stack:** Bun + TypeScript（strict）+ bun:sqlite + LanceDB + zod（schema 校验）+ bun:test。Spec: `docs/superpowers/specs/2026-07-01-fsck-design.md`。

---

## 实现勘误（spec 调研后校正，以本 plan 为准）

spec 草案基于命名推测写了几个不存在的 API，调研后校正：
1. **`db.getAllPages` → `db.listPages(opts?)`**；**`db.getPageBySlug` → `db.getPage(slug)`**。
2. **FK 孤儿复用 `db.checkFkViolations(): { byTable: Record<string,number>; total: number }`**（基于 `PRAGMA foreign_key_check`，只读）——不新写 SQL。
3. **没有 profile DB 表**（profile 是 YAML 文件）→ 检查项 #10 实际查 `aliases` + `checkFkViolations()` 返回的其他 derived 表。`profile` 字样从 #10 移除。
4. **`cbrain repair-fk --execute` 覆盖整个 `DERIVED_FK_TABLES` 白名单**（aliases/chunks/links/mention_snapshots/tags/timeline/versions）→ #8/9/10/aliases 都用它，**不拆分**，suggestedCommand 可达。
5. **quarantine 存 `config` 表 key=`watcher.quarantine`**（JSON）→ 检查项 #11 读 `config` 表解析。

检查项最终映射（severity 经 review 调整后）：

| # | check id | probe | severity | suggestedCommand |
|---|---|---|---|---|
| 1 | `vault.file_exists_db_missing` | vault | `error` | `cbrain sync --slug <slug> --reindex` |
| 2 | `vault.db_exists_file_missing` | vault | `error` | `cbrain show <slug>`（确认 DB body 是否还在）|
| 3 | `vault.frontmatter_slug_mismatch` | vault | `warning` | `cbrain sync --slug <slug> --reindex` |
| 4 | `sqlite.title_collision` | sqlite | `error` | `cbrain doctor`（诊断/隔离入口）|
| 5 | `sqlite.page_without_chunks` | sqlite | `warning` | `cbrain sync --slug <slug> --reindex` |
| 6 | `fts.coverage_gap` | fts | `warning` | `cbrain sync --slug <slug> --reindex`（局部）/ `cbrain doctor`（全局）|
| 7 | `lance.vector_coverage_gap` | lance | `error` | `cbrain sync --reindex-vectors` |
| 8 | `sqlite.orphan_links` | sqlite | `warning` | `cbrain repair-fk --execute` |
| 9 | `sqlite.orphan_timeline` | sqlite | `warning` | `cbrain repair-fk --execute` |
| 10 | `sqlite.orphan_aliases` | sqlite | `warning` | `cbrain repair-fk --execute` |
| 11 | `sqlite.quarantine_context` | sqlite | `info` | —（仅 context）|

> `checkFkViolations()` 若还返回 tags/versions/mention_snapshots/chunks 的孤儿，sqlite-probe 额外产出 `sqlite.orphan_<table>` finding（同 `repair-fk --execute`），不丢弃。

## File Structure

- **Create** `src/core/fsck/types.ts` — 稳定数据契约 + zod schema
- **Create** `src/core/fsck/report.ts` — 聚合 findings → FsckReport、overallStatus 计算、匿名化、human markdown
- **Create** `src/core/fsck/sqlite-probe.ts` — title collision / page_without_chunks / FK 孤儿 / quarantine context
- **Create** `src/core/fsck/fts-probe.ts` — chunks_fts 覆盖
- **Create** `src/core/fsck/lance-probe.ts` — 向量覆盖（只读，不建表/目录）
- **Create** `src/core/fsck/vault-probe.ts` — vault ↔ SQLite 对齐 + frontmatter mismatch
- **Create** `src/cli/commands/fsck.ts` — CLI 编排（flag / 跑 probe / 输出 / exit code）
- **Modify** `src/cli/program.ts` — 注册 `fsck` 命令
- **Test** `tests/core/fsck/{types,report,sqlite-probe,fts-probe,lance-probe,vault-probe}.test.ts` + `tests/cli/fsck.test.ts`

每个 probe 是纯函数：`(db, opts) => FsckFinding[]`（lance 异步）。无副作用、易测、易组合。

---

## Task 1: types + zod schema

**Files:**
- Create: `src/core/fsck/types.ts`
- Test: `tests/core/fsck/types.test.ts`

- [ ] **Step 1: 写失败测试**

```typescript
// tests/core/fsck/types.test.ts
import { test, expect } from "bun:test";
import { FsckReportSchema, FsckSeveritySchema } from "../../../src/core/fsck/types.js";

test("FsckSeveritySchema accepts 4 levels", () => {
  for (const s of ["critical", "error", "warning", "info"] as const) {
    expect(FsckSeveritySchema.safeParse(s).success).toBe(true);
  }
  expect(FsckSeveritySchema.safeParse("high").success).toBe(false); // 非 health 语义
});

test("FsckReportSchema parses a clean report", () => {
  const r = {
    version: 1,
    timestamp: "2026-07-01T00:00:00.000Z",
    overallStatus: "pass",
    counts: { critical: 0, error: 0, warning: 0, info: 0 },
    lanceState: "ok",
    findings: [],
  };
  expect(FsckReportSchema.safeParse(r).success).toBe(true);
});

test("FsckReportSchema rejects unknown severity in findings", () => {
  const r = { version: 1, timestamp: "t", overallStatus: "pass",
    counts: { critical: 0, error: 0, warning: 0, info: 0 }, lanceState: "ok",
    findings: [{ check: "x", layer: "sqlite", severity: "HIGH", count: 1, sampleSlugs: [], detail: "d", suggestedCommand: "" }] };
  expect(FsckReportSchema.safeParse(r).success).toBe(false);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `bun test tests/core/fsck/types.test.ts`
Expected: FAIL — `Cannot find module .../types.js`

- [ ] **Step 3: 写 types.ts**

```typescript
// src/core/fsck/types.ts
import { z } from "zod";

export const FsckSeveritySchema = z.enum(["critical", "error", "warning", "info"]);
export type FsckSeverity = z.infer<typeof FsckSeveritySchema>;

export const FsckLayerSchema = z.enum(["vault", "sqlite", "fts", "lance"]);
export type FsckLayer = z.infer<typeof FsckLayerSchema>;

export const FsckOverallStatusSchema = z.enum(["pass", "warn", "fail"]);
export type FsckOverallStatus = z.infer<typeof FsckOverallStatusSchema>;

export const FsckLanceStateSchema = z.enum(["ok", "missing", "corrupt", "unchecked"]);
export type FsckLanceState = z.infer<typeof FsckLanceStateSchema>;

export const FsckFindingSchema = z.object({
  check: z.string(),
  layer: FsckLayerSchema,
  severity: FsckSeveritySchema,
  count: z.number().int().nonnegative(),
  sampleSlugs: z.array(z.string()).max(5),
  detail: z.string(),
  suggestedCommand: z.string(),
});
export type FsckFinding = z.infer<typeof FsckFindingSchema>;

export const FsckReportSchema = z.object({
  version: z.literal(1),
  timestamp: z.string(),
  overallStatus: FsckOverallStatusSchema,
  counts: z.object({ critical: z.number(), error: z.number(), warning: z.number(), info: z.number() }),
  lanceState: FsckLanceStateSchema,
  fatalError: z.string().optional(),
  findings: z.array(FsckFindingSchema),
});
export type FsckReport = z.infer<typeof FsckReportSchema>;
```

- [ ] **Step 4: 跑测试确认通过**

Run: `bun test tests/core/fsck/types.test.ts`
Expected: PASS（3/3）

- [ ] **Step 5: Commit**

```bash
git add src/core/fsck/types.ts tests/core/fsck/types.test.ts
git commit -m "feat(fsck): stable types + zod schema (#262)"
```

---

## Task 2: report 聚合 + overallStatus + 匿名化 + human 渲染

**Files:**
- Create: `src/core/fsck/report.ts`
- Test: `tests/core/fsck/report.test.ts`

- [ ] **Step 1: 写失败测试**

```typescript
// tests/core/fsck/report.test.ts
import { test, expect } from "bun:test";
import { buildReport, anonymizeSlugs, severityToStatus, reportToMarkdown } from "../../../src/core/fsck/report.js";
import type { FsckFinding } from "../../../src/core/fsck/types.js";

const f = (severity: FsckFinding["severity"]): FsckFinding => ({
  check: "x", layer: "sqlite", severity, count: 1, sampleSlugs: ["a"], detail: "d", suggestedCommand: "c",
});

test("severityToStatus: critical/error → fail, warning → warn, info/pass → pass", () => {
  expect(severityToStatus(["critical"])).toBe("fail");
  expect(severityToStatus(["error"])).toBe("fail");
  expect(severityToStatus(["warning"])).toBe("warn");
  expect(severityToStatus(["info"])).toBe("pass");
  expect(severityToStatus([])).toBe("pass");
});

test("buildReport aggregates counts + status; fatalError forces fail", () => {
  const r = buildReport([f("warning"), f("info")], "ok", "2026-07-01T00:00:00Z");
  expect(r.overallStatus).toBe("warn");
  expect(r.counts).toEqual({ critical: 0, error: 0, warning: 1, info: 1 });
  const failed = buildReport([f("info")], "ok", "t", "DB unreadable");
  expect(failed.overallStatus).toBe("fail");
  expect(failed.fatalError).toBe("DB unreadable");
});

test("anonymizeSlugs truncates to 5 + caps long slug length", () => {
  const many = Array.from({ length: 10 }, (_, i) => `slug-${i}`.repeat(8));
  const out = anonymizeSlugs(many);
  expect(out).toHaveLength(5);
  for (const s of out) expect(s.length).toBeLessThanOrEqual(40);
});

test("reportToMarkdown groups by layer and lists suggestedCommand", () => {
  const r = buildReport([f("warning")], "ok", "t");
  const md = reportToMarkdown(r);
  expect(md).toContain("sqlite");          // layer 分组
  expect(md).toContain("c");               // suggestedCommand
  expect(md).not.toContain("/Users/");     // 无绝对路径
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `bun test tests/core/fsck/report.test.ts`
Expected: FAIL — module not found

- [ ] **Step 3: 写 report.ts**

```typescript
// src/core/fsck/report.ts
import type { FsckFinding, FsckLanceState, FsckOverallStatus, FsckReport, FsckSeverity } from "./types.js";

const RANK: Record<FsckSeverity, number> = { critical: 3, error: 2, warning: 1, info: 0 };

export function severityToStatus(severities: FsckSeverity[]): FsckOverallStatus {
  let max = -1;
  for (const s of severities) max = Math.max(max, RANK[s]);
  if (max >= 2) return "fail";   // critical 或 error
  if (max === 1) return "warn";  // warning
  return "pass";
}

const MAX_SLUG_LEN = 40;
export function anonymizeSlugs(slugs: string[], max = 5): string[] {
  return slugs.slice(0, max).map((s) => (s.length > MAX_SLUG_LEN ? `${s.slice(0, MAX_SLUG_LEN - 1)}…` : s));
}

export function buildReport(
  findings: FsckFinding[],
  lanceState: FsckLanceState,
  timestamp: string,
  fatalError?: string,
): FsckReport {
  const counts = { critical: 0, error: 0, warning: 0, info: 0 };
  for (const f of findings) counts[f.severity]++;
  const overallStatus: FsckOverallStatus = fatalError ? "fail" : severityToStatus(findings.map((f) => f.severity));
  const report: FsckReport = { version: 1, timestamp, overallStatus, counts, lanceState, findings };
  if (fatalError) report.fatalError = fatalError;
  return report;
}

export function reportToMarkdown(report: FsckReport): string {
  const lines: string[] = [];
  lines.push(`# fsck report\n`);
  lines.push(`- overall: **${report.overallStatus}**  •  lance: ${report.lanceState}`);
  lines.push(`- counts: critical=${report.counts.critical} error=${report.counts.error} warning=${report.counts.warning} info=${report.counts.info}\n`);
  if (report.fatalError) lines.push(`> ⚠ fatalError: ${report.fatalError}\n`);
  const byLayer = new Map<string, FsckFinding[]>();
  for (const f of report.findings) {
    if (!byLayer.has(f.layer)) byLayer.set(f.layer, []);
    byLayer.get(f.layer)!.push(f);
  }
  for (const [layer, fs] of byLayer) {
    lines.push(`## ${layer}`);
    for (const f of fs) {
      lines.push(`- [${f.severity}] ${f.check}: ${f.detail} (${f.count})`);
      if (f.sampleSlugs.length) lines.push(`  sample: ${f.sampleSlugs.join(", ")}`);
      if (f.suggestedCommand) lines.push(`  → \`${f.suggestedCommand}\`（仅建议，不执行）`);
    }
    lines.push("");
  }
  return lines.join("\n");
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `bun test tests/core/fsck/report.test.ts`
Expected: PASS（4/4）

- [ ] **Step 5: Commit**

```bash
git add src/core/fsck/report.ts tests/core/fsck/report.test.ts
git commit -m "feat(fsck): report aggregation, severity→status, anonymization, markdown (#262)"
```

---

## Task 3: sqlite-probe（title collision / page_without_chunks / FK 孤儿 / quarantine context）

**Files:**
- Create: `src/core/fsck/sqlite-probe.ts`
- Test: `tests/core/fsck/sqlite-probe.test.ts`

> 测试用真 `CBrainDB`（`/tmp/cbrain-test-*`，`afterEach` 清理）。构造 fixture：用合成 sentinel slug（`test-entity-a` 等），绝不写真名/路径/email。

- [ ] **Step 1: 写失败测试**

```typescript
// tests/core/fsck/sqlite-probe.test.ts
import { test, expect, afterEach } from "bun:test";
import { mkdtempSync, rmSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { CBrainDB } from "../../../src/storage/sqlite.js";
import { probeSqlite } from "../../../src/core/fsck/sqlite-probe.js";

let dir: string;
let db: CBrainDB;
afterEach(() => { db?.close(); if (dir) rmSync(dir, { recursive: true, force: true }); });

function freshDb(): CBrainDB {
  dir = mkdtempSync(join(tmpdir(), "cbrain-fsck-sql-"));
  db = new CBrainDB(join(dir, "t.sqlite"));
  return db;
}

test("title collision: two pages same title → error finding", () => {
  const d = freshDb();
  d.upsertPage({ slug: "a", type: "entity", title: "dup", file_path: "a.md", content: "x" } as any);
  d.upsertPage({ slug: "b", type: "entity", title: "dup", file_path: "b.md", content: "x" } as any);
  const fs = probeSqlite(d);
  const col = fs.find((f) => f.check === "sqlite.title_collision");
  expect(col?.severity).toBe("error");
  expect(col?.count).toBe(1);
});

test("page without chunks → warning finding", () => {
  const d = freshDb();
  d.upsertPage({ slug: "no-chunk", type: "entity", title: "t", file_path: "n.md", content: "x" } as any);
  const fs = probeSqlite(d);
  expect(fs.find((f) => f.check === "sqlite.page_without_chunks")?.severity).toBe("warning");
});

test("FK orphan links via checkFkViolations → orphan_links warning + repair-fk suggested", () => {
  const d = freshDb();
  // 直接插一条 from_slug 指向不存在 page 的 links（绕过 FK？需先禁 FK 或插 page 再删 page）
  d.upsertPage({ slug: "p1", type: "entity", title: "t1", file_path: "p1.md", content: "x" } as any);
  d.upsertPage({ slug: "p2", type: "entity", title: "t2", file_path: "p2.md", content: "x" } as any);
  d.addLink("p1", "p2", "mentions");
  d.deletePage("p2"); // CASCADE 应删 link；若 FK 关闭则 link 成孤儿
  const fs = probeSqlite(d);
  const o = fs.find((f) => f.check === "sqlite.orphan_links");
  if (o) expect(o.suggestedCommand).toBe("cbrain repair-fk --execute");
  // 注：若 CASCADE 生效无孤儿，o 为 undefined，也接受（checkFkViolations 返回空）
});

test("quarantine context read from config table → info finding or none", () => {
  const d = freshDb();
  d.setConfig("watcher.quarantine", JSON.stringify([{ slug: "q1", quarantinedAt: "t", reason: "flux" }]));
  const fs = probeSqlite(d);
  const q = fs.find((f) => f.check === "sqlite.quarantine_context");
  expect(q?.severity).toBe("info");
});
```

> **实现者注**：`upsertPage`/`addLink`/`deletePage`/`setConfig` 的精确签名以 `src/storage/sqlite.ts` 为准——若名字不同（如 `setConfig` 实为 `setConfigValue`），按实际改名。测试用 `as any` 构造 PageRow 是临时的，实现时按真实 PageRow 字段填。

- [ ] **Step 2: 跑测试确认失败**

Run: `bun test tests/core/fsck/sqlite-probe.test.ts`
Expected: FAIL — module not found / 方法名待核

- [ ] **Step 3: 先 grep 确认 db 方法真实签名**

Run: `grep -nE "setConfig|setConfigValue|upsertPage|insertPage|addLink|deletePage" src/storage/sqlite.ts`
→ 用真实方法名替换测试 stub。重跑测试仍 FAIL（probeSqlite 未实现）。

- [ ] **Step 4: 写 sqlite-probe.ts**

```typescript
// src/core/fsck/sqlite-probe.ts
import type { CBrainDB } from "../../storage/sqlite.js";
import type { FsckFinding } from "./types.js";
import { anonymizeSlugs } from "./report.js";

export function probeSqlite(db: CBrainDB): FsckFinding[] {
  const findings: FsckFinding[] = [];

  // #4 title collision
  const dupTitles = db.prepare(
    "SELECT title, COUNT(*) c, GROUP_CONCAT(slug) slugs FROM pages GROUP BY title HAVING c > 1",
  ).all() as Array<{ title: string; c: number; slugs: string }>;
  if (dupTitles.length) {
    const count = dupTitles.reduce((n, r) => n + r.c, 0);
    findings.push({
      check: "sqlite.title_collision", layer: "sqlite", severity: "error", count,
      sampleSlugs: anonymizeSlugs(dupTitles.flatMap((r) => r.slugs.split(","))),
      detail: `${dupTitles.length} 个标题被多个 page 共用（cbrain doctor 为诊断/隔离入口，不自动修复）`,
      suggestedCommand: "cbrain doctor",
    });
  }

  // #5 page without chunks
  const noChunks = db.prepare(
    "SELECT p.slug FROM pages p LEFT JOIN (SELECT DISTINCT page_slug FROM chunks) c ON p.slug = c.page_slug WHERE c.page_slug IS NULL",
  ).all() as Array<{ slug: string }>;
  if (noChunks.length) {
    findings.push({
      check: "sqlite.page_without_chunks", layer: "sqlite", severity: "warning", count: noChunks.length,
      sampleSlugs: anonymizeSlugs(noChunks.map((r) => r.slug)),
      detail: "page 没有 chunks（无法检索）", suggestedCommand: "cbrain sync --slug <slug> --reindex",
    });
  }

  // #8/9/10 + 其他 derived FK 孤儿：复用 checkFkViolations（只读）
  const fk = db.checkFkViolations();
  for (const [table, n] of Object.entries(fk.byTable)) {
    findings.push({
      check: `sqlite.orphan_${table}`, layer: "sqlite", severity: "warning", count: n,
      sampleSlugs: [], // checkFkViolations 只给 count；sample 需额外查询，留空避免过度查询
      detail: `${table} 表有 ${n} 行指向不存在的 page（FK 孤儿）`,
      suggestedCommand: "cbrain repair-fk --execute",
    });
  }

  // #11 quarantine context（info，不计 fail）
  const qrow = db.prepare("SELECT value FROM config WHERE key = 'watcher.quarantine'").get() as { value: string } | undefined;
  if (qrow) {
    try {
      const list = JSON.parse(qrow.value) as Array<{ slug?: string }>;
      findings.push({
        check: "sqlite.quarantine_context", layer: "sqlite", severity: "info", count: list.length,
        sampleSlugs: anonymizeSlugs(list.map((x) => x.slug ?? "").filter(Boolean)),
        detail: "watcher 隔离中的 page（context，非故障）", suggestedCommand: "",
      });
    } catch { /* 损坏的 JSON 不报，避免噪音 */ }
  }

  return findings;
}
```

> **注**：`db.prepare(...)` 是 CBrainDB 暴露的只读查询入口（测试直接访问私有 prepare，存在已知类型错误，CLAUDE.md 记录在案，非本任务阻塞）。所有查询为 `SELECT`，零写。

- [ ] **Step 5: 跑测试确认通过**

Run: `bun test tests/core/fsck/sqlite-probe.test.ts`
Expected: PASS（4/4）

- [ ] **Step 6: Commit**

```bash
git add src/core/fsck/sqlite-probe.ts tests/core/fsck/sqlite-probe.test.ts
git commit -m "feat(fsck): sqlite probe (title collision, chunks coverage, FK orphans, quarantine) (#262)"
```

---

## Task 4: fts-probe（chunks_fts 覆盖）

**Files:**
- Create: `src/core/fsck/fts-probe.ts`
- Test: `tests/core/fsck/fts-probe.test.ts`

- [ ] **Step 1: 写失败测试**

```typescript
// tests/core/fsck/fts-probe.test.ts
import { test, expect, afterEach } from "bun:test";
import { mkdtempSync, rmSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { CBrainDB } from "../../../src/storage/sqlite.js";
import { probeFts } from "../../../src/core/fsck/fts-probe.js";

let dir: string; let db: CBrainDB;
afterEach(() => { db?.close(); if (dir) rmSync(dir, { recursive: true, force: true }); });

test("page with chunks but missing from chunks_fts → coverage_gap warning, sync suggested (not reindex-vectors)", () => {
  dir = mkdtempSync(join(tmpdir(), "cbrain-fsck-fts-"));
  db = new CBrainDB(join(dir, "t.sqlite"));
  db.upsertPage({ slug: "p", type: "entity", title: "t", file_path: "p.md", content: "x" } as any);
  db.addChunk?.("p", 0, "hello world"); // 按 grep 出的真实方法名替换
  // 不写 chunks_fts → 制造缺口
  const fs = probeFts(db);
  const gap = fs.find((f) => f.check === "fts.coverage_gap");
  expect(gap?.severity).toBe("warning");
  expect(gap?.suggestedCommand).not.toContain("reindex-vectors");
});

test("clean: chunks and fts in sync → no finding", () => {
  dir = mkdtempSync(join(tmpdir(), "cbrain-fsck-fts2-"));
  db = new CBrainDB(join(dir, "t.sqlite"));
  expect(probeFts(db)).toEqual([]);
});
```

- [ ] **Step 2: 跑测试确认失败** — Run: `bun test tests/core/fsck/fts-probe.test.ts` → FAIL module not found。

- [ ] **Step 3: grep 确认 chunks 写入方法 + fts 同步机制**

Run: `grep -nE "INSERT INTO chunks|chunks_fts.*INSERT|addChunk|insertChunk|upsertChunk|reindexChunk" src/storage/sqlite.ts`
→ 确认 chunks 写入时是否自动同步 chunks_fts（L1106 附近 `INSERT INTO chunks_fts`）。测试 fixture 按真实写入路径构造（让 DB 自己同步 fts），再手动 `DELETE FROM chunks_fts WHERE page_slug=?` 制造缺口。按真实方法名替换 `db.addChunk?.(...)`。

- [ ] **Step 4: 写 fts-probe.ts**

```typescript
// src/core/fsck/fts-probe.ts
import type { CBrainDB } from "../../storage/sqlite.js";
import type { FsckFinding } from "./types.js";
import { anonymizeSlugs } from "./report.js";

export function probeFts(db: CBrainDB): FsckFinding[] {
  // 有 chunks 但 chunks_fts 没有的 page_slug
  const gaps = db.prepare(
    `SELECT DISTINCT c.page_slug FROM chunks c
     LEFT JOIN chunks_fts f ON f.page_slug = c.page_slug
     WHERE f.page_slug IS NULL`,
  ).all() as Array<{ page_slug: string }>;
  if (!gaps.length) return [];
  return [{
    check: "fts.coverage_gap", layer: "fts", severity: "warning", count: gaps.length,
    sampleSlugs: anonymizeSlugs(gaps.map((r) => r.page_slug)),
    detail: "FTS 索引缺口：有 chunks 但未进 chunks_fts（走 cbrain sync 重建 chunks 顺带 FTS，非 --reindex-vectors）",
    suggestedCommand: gaps.length <= 5 ? "cbrain sync --slug <slug> --reindex" : "cbrain doctor",
  }];
}
```

- [ ] **Step 5: 跑测试确认通过** — Run: `bun test tests/core/fsck/fts-probe.test.ts` → PASS。

- [ ] **Step 6: Commit**

```bash
git add src/core/fsck/fts-probe.ts tests/core/fsck/fts-probe.test.ts
git commit -m "feat(fsck): fts coverage probe (#262)"
```

---

## Task 5: lance-probe（向量覆盖，只读不建表/目录）

**Files:**
- Create: `src/core/fsck/lance-probe.ts`
- Test: `tests/core/fsck/lance-probe.test.ts`

> 技术核心：`existsSync(lancePath)` 预检（不建目录）→ `LanceDBManager.connect()`（目录已存在，幂等）→ `openChunksStrict()`（表不存在抛 `LanceTableMissingError`，不建表）→ `query().select(["pageSlug"])` 只读聚合。**禁用** `warmup`/`getOrCreateTable`/`createTable`。

- [ ] **Step 1: 写失败测试**

```typescript
// tests/core/fsck/lance-probe.test.ts
import { test, expect, afterEach } from "bun:test";
import { mkdtempSync, rmSync, existsSync, mkdirSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { CBrainDB } from "../../../src/storage/sqlite.js";
import { probeLance } from "../../../src/core/fsck/lance-probe.js";

let dir: string; let db: CBrainDB;
afterEach(() => { db?.close(); if (dir) rmSync(dir, { recursive: true, force: true }); });

test("lancePath not exist → lanceState unchecked, directory NOT created, no finding", async () => {
  dir = mkdtempSync(join(tmpdir(), "cbrain-fsck-lance1-"));
  db = new CBrainDB(join(dir, "t.sqlite"));
  const missingLance = join(dir, "does-not-exist");
  const res = await probeLance(missingLance, db);
  expect(res.state).toBe("unchecked");
  expect(existsSync(missingLance)).toBe(false); // 关键：未创建目录
  expect(res.findings).toEqual([]);
});

test("lancePath exists but chunks table missing → lanceState missing, no finding", async () => {
  dir = mkdtempSync(join(tmpdir(), "cbrain-fsck-lance2-"));
  db = new CBrainDB(join(dir, "t.sqlite"));
  const lanceDir = join(dir, "lance"); mkdirSync(lanceDir);
  const res = await probeLance(lanceDir, db);
  expect(["missing", "corrupt"]).toContain(res.state); // 空目录无表
  expect(res.findings).toEqual([]);
});
```

> 完整向量覆盖测试（建表 + 插向量 + 对比）较重，作为集成测试可选；上面两个是 issue 硬要求的「不建目录/表」安全断言，必做。

- [ ] **Step 2: 跑测试确认失败** — Run: `bun test tests/core/fsck/lance-probe.test.ts` → FAIL。

- [ ] **Step 3: 确认 LanceTableMissingError 导出路径**

Run: `grep -nE "LanceTableMissingError|export class Lance|openChunksStrict" src/storage/lancedb.ts`
→ 确认 `LanceTableMissingError` 的 import 路径（`../../storage/lancedb.js` 或子模块）。

- [ ] **Step 4: 写 lance-probe.ts**

```typescript
// src/core/fsck/lance-probe.ts
import { existsSync } from "node:fs";
import { LanceDBManager, LanceTableMissingError } from "../../storage/lancedb.js"; // 按实际导出调整
import type { CBrainDB } from "../../storage/sqlite.js";
import type { FsckFinding, FsckLanceState } from "./types.js";
import { anonymizeSlugs } from "./report.js";

export async function probeLance(lancePath: string, db: CBrainDB): Promise<{ findings: FsckFinding[]; state: FsckLanceState }> {
  if (!existsSync(lancePath)) return { findings: [], state: "unchecked" }; // 不 connect，不建目录
  const mgr = new LanceDBManager();
  try {
    await mgr.connect(lancePath);          // 目录已存在，幂等，不新建
    const table = await mgr.openChunksStrict(); // 表缺失抛错，不建表
    // 只读：拉全部向量行的 pageSlug
    const rows = await table.query().select(["pageSlug"]).toArray() as Array<{ pageSlug: string }>;
    const vectorSlugs = new Set(rows.map((r) => r.pageSlug));
    // 对比 SQLite chunks 有但向量没有的 page
    const chunkSlugs = db.prepare("SELECT DISTINCT page_slug FROM chunks").all() as Array<{ page_slug: string }>;
    const missing = chunkSlugs.filter((r) => !vectorSlugs.has(r.page_slug));
    if (!missing.length) return { findings: [], state: "ok" };
    return {
      state: "ok",
      findings: [{
        check: "lance.vector_coverage_gap", layer: "lance", severity: "error", count: missing.length,
        sampleSlugs: anonymizeSlugs(missing.map((r) => r.page_slug)),
        detail: "page 有 chunks 但 LanceDB 无向量（recall 受损）",
        suggestedCommand: "cbrain sync --reindex-vectors",
      }],
    };
  } catch (e) {
    if (e instanceof LanceTableMissingError) return { findings: [], state: "missing" };
    return { findings: [], state: "corrupt" };
  }
}
```

> **只读断言**（实现者自查）：本函数除 `connect`（已 existsSync 预检）外无任何 create/write 调用；`query().select()` 是 LanceDB 只读 API。

- [ ] **Step 5: 跑测试确认通过** — Run: `bun test tests/core/fsck/lance-probe.test.ts` → PASS。

- [ ] **Step 6: Commit**

```bash
git add src/core/fsck/lance-probe.ts tests/core/fsck/lance-probe.test.ts
git commit -m "feat(fsck): lance vector coverage probe (read-only, no table/dir creation) (#262)"
```

---

## Task 6: vault-probe（vault ↔ SQLite 对齐 + frontmatter mismatch）

**Files:**
- Create: `src/core/fsck/vault-probe.ts`
- Test: `tests/core/fsck/vault-probe.test.ts`

- [ ] **Step 1: 写失败测试**

```typescript
// tests/core/fsck/vault-probe.test.ts
import { test, expect, afterEach } from "bun:test";
import { mkdtempSync, rmSync, writeFileSync, mkdirSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { CBrainDB } from "../../../src/storage/sqlite.js";
import { probeVault } from "../../../src/core/fsck/vault-probe.js";

let dir: string; let vault: string; let db: CBrainDB;
afterEach(() => { db?.close(); if (dir) rmSync(dir, { recursive: true, force: true }); });

function setup() {
  dir = mkdtempSync(join(tmpdir(), "cbrain-fsck-vault-"));
  vault = join(dir, "vault"); mkdirSync(join(vault, "entities"), { recursive: true });
  db = new CBrainDB(join(dir, "t.sqlite"));
}

test("md file exists but DB missing → file_exists_db_missing error", () => {
  setup();
  writeFileSync(join(vault, "entities", "orphan.md"), "---\nslug: orphan-entity\n---\nbody");
  const fs = probeVault(vault, db);
  expect(fs.find((f) => f.check === "vault.file_exists_db_missing")?.severity).toBe("error");
});

test("DB page exists but file missing → db_exists_file_missing error + show <slug>", () => {
  setup();
  db.upsertPage({ slug: "ghost", type: "entity", title: "g", file_path: "entities/ghost.md", content: "x" } as any);
  // 不创建 ghost.md
  const fs = probeVault(vault, db);
  const f = fs.find((x) => x.check === "vault.db_exists_file_missing");
  expect(f?.severity).toBe("error");
  expect(f?.suggestedCommand).toContain("cbrain show");
});

test("frontmatter slug mismatch → warning", () => {
  setup();
  writeFileSync(join(vault, "entities", "m.md"), "---\nslug: frontmatter-slug\n---\nbody");
  db.upsertPage({ slug: "db-slug", type: "entity", title: "m", file_path: "entities/m.md", content: "x" } as any);
  const fs = probeVault(vault, db);
  expect(fs.find((x) => x.check === "vault.frontmatter_slug_mismatch")?.severity).toBe("warning");
});
```

- [ ] **Step 2: 跑测试确认失败** — Run: `bun test tests/core/fsck/vault-probe.test.ts` → FAIL。

- [ ] **Step 3: 确认 frontmatter 解析 util + PageRow 路径列**

Run: `grep -nE "export function parse|parseFrontmatter|extractSlug|frontmatter" src/utils/frontmatter.ts src/utils/slug.ts`
→ 用真实 frontmatter 解析函数（提取 `slug` 字段）。

- [ ] **Step 4: 写 vault-probe.ts**

```typescript
// src/core/fsck/vault-probe.ts
import { readdirSync, readFileSync, existsSync } from "node:fs";
import { join, relative } from "node:path";
import type { CBrainDB } from "../../storage/sqlite.js";
import type { FsckFinding } from "./types.js";
import { anonymizeSlugs } from "./report.js";
import { parseFrontmatter } from "../../utils/frontmatter.js"; // 按实际导出调整

function walkMd(root: string): string[] {
  const out: string[] = [];
  for (const ent of readdirSync(root, { withFileTypes: true })) {
    const p = join(root, ent.name);
    if (ent.isDirectory()) out.push(...walkMd(p));
    else if (ent.name.endsWith(".md")) out.push(p);
  }
  return out;
}

export function probeVault(vaultPath: string, db: CBrainDB): FsckFinding[] {
  const findings: FsckFinding[] = [];
  const pages = db.listPages(); // PageRow[]，含 slug + file_path
  const dbBySlug = new Map(pages.map((p) => [p.slug, p]));

  // #1 文件存在 DB 缺失；#3 frontmatter mismatch
  const fileExistsDbMissing: string[] = [];
  const mismatch: string[] = [];
  for (const abs of walkMd(vaultPath)) {
    const rel = relative(vaultPath, abs);
    const raw = readFileSync(abs, "utf-8");
    const fm = parseFrontmatter(raw); // { slug?: string }
    const fmSlug = fm?.slug;
    if (!fmSlug) continue; // 无 slug frontmatter，跳过（非 cbrain 页）
    const dbPage = dbBySlug.get(fmSlug);
    if (!dbPage) { fileExistsDbMissing.push(fmSlug); continue; }
    if (dbPage.file_path !== rel) mismatch.push(fmSlug); // #3 path 不一致
  }
  if (fileExistsDbMissing.length) findings.push({
    check: "vault.file_exists_db_missing", layer: "vault", severity: "error", count: fileExistsDbMissing.length,
    sampleSlugs: anonymizeSlugs(fileExistsDbMissing),
    detail: "vault 有 .md 但 DB 无对应 page", suggestedCommand: "cbrain sync --slug <slug> --reindex",
  });
  if (mismatch.length) findings.push({
    check: "vault.frontmatter_slug_mismatch", layer: "vault", severity: "warning", count: mismatch.length,
    sampleSlugs: anonymizeSlugs(mismatch),
    detail: "frontmatter slug/path 与 DB 不一致", suggestedCommand: "cbrain sync --slug <slug> --reindex",
  });

  // #2 DB 存在文件缺失
  const dbExistsFileMissing = pages.filter((p) => p.file_path && !existsSync(join(vaultPath, p.file_path)));
  if (dbExistsFileMissing.length) findings.push({
    check: "vault.db_exists_file_missing", layer: "vault", severity: "error", count: dbExistsFileMissing.length,
    sampleSlugs: anonymizeSlugs(dbExistsFileMissing.map((p) => p.slug)),
    detail: "DB 有 page 但 vault 文件缺失（DB body 仍在 → backup 恢复 / re-ingest / manual writeback）",
    suggestedCommand: "cbrain show <slug>",
  });

  return findings;
}
```

- [ ] **Step 5: 跑测试确认通过** — Run: `bun test tests/core/fsck/vault-probe.test.ts` → PASS（3/3）。

- [ ] **Step 6: Commit**

```bash
git add src/core/fsck/vault-probe.ts tests/core/fsck/vault-probe.test.ts
git commit -m "feat(fsck): vault ↔ sqlite alignment probe (#262)"
```

---

## Task 7: CLI 编排（fsck.ts）+ 注册 + exit code

**Files:**
- Create: `src/cli/commands/fsck.ts`
- Modify: `src/cli/program.ts`（注册 fsck）
- Test: `tests/cli/fsck.test.ts`

- [ ] **Step 1: 写失败测试**

```typescript
// tests/cli/fsck.test.ts
import { test, expect, afterEach } from "bun:test";
import { mkdtempSync, rmSync, mkdirSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { CBrainDB } from "../../src/storage/sqlite.js";
import { runFsck } from "../../src/cli/commands/fsck.js";

let dir: string;
afterEach(() => { if (dir) rmSync(dir, { recursive: true, force: true }); });

test("clean brain → exit 0, overallStatus pass", async () => {
  dir = mkdtempSync(join(tmpdir(), "cbrain-fsck-cli-"));
  const db = new CBrainDB(join(dir, "t.sqlite"));
  const res = await runFsck({ vaultPath: join(dir, "vault"), lancePath: join(dir, "lance"), db });
  db.close();
  expect(res.report.overallStatus).toBe("pass");
  expect(res.exitCode).toBe(0);
});

test("title collision → exit 1, overallStatus fail", async () => {
  dir = mkdtempSync(join(tmpdir(), "cbrain-fsck-cli2-"));
  const db = new CBrainDB(join(dir, "t.sqlite"));
  db.upsertPage({ slug: "a", type: "entity", title: "dup", file_path: "a.md", content: "x" } as any);
  db.upsertPage({ slug: "b", type: "entity", title: "dup", file_path: "b.md", content: "x" } as any);
  const res = await runFsck({ vaultPath: join(dir, "vault"), lancePath: join(dir, "lance"), db });
  db.close();
  expect(res.report.overallStatus).toBe("fail");
  expect(res.exitCode).toBe(1);
});

test("DB unreadable (simulated) → exit 2 + fatalError", async () => {
  // runFsck 捕获 probe 抛错 → fatalError + exit 2
  dir = mkdtempSync(join(tmpdir(), "cbrain-fsck-cli3-"));
  const db = new CBrainDB(join(dir, "t.sqlite"));
  db.close();
  const res = await runFsck({ vaultPath: join(dir, "vault"), lancePath: join(dir, "lance"), db: closedHandle() });
  expect(res.exitCode).toBe(2);
  expect(res.report.fatalError).toBeTruthy();
});

function closedHandle(): CBrainDB { /* 返回一个已 close 的 db，触发 prepare 抛错 */ return null as any; }
```

- [ ] **Step 2: 跑测试确认失败** — Run: `bun test tests/cli/fsck.test.ts` → FAIL。

- [ ] **Step 3: 写 fsck.ts（含 runFsck 核心可测函数 + commander 注册）**

```typescript
// src/cli/commands/fsck.ts
import type { Command } from "commander";
import type { CBrainDB } from "../../storage/sqlite.js";
import type { FsckFinding, FsckReport, FsckLayer, FsckLanceState } from "../../core/fsck/types.js";
import { buildReport, reportToMarkdown } from "../../core/fsck/report.js";
import { probeSqlite } from "../../core/fsck/sqlite-probe.js";
import { probeFts } from "../../core/fsck/fts-probe.js";
import { probeLance } from "../../core/fsck/lance-probe.js";
import { probeVault } from "../../core/fsck/vault-probe.js";

export interface FsckInput { vaultPath: string; lancePath: string; db: CBrainDB; layer?: FsckLayer; }
export interface FsckResult { report: FsckReport; exitCode: 0 | 1 | 2; }

const ALL_LAYERS: FsckLayer[] = ["vault", "sqlite", "fts", "lance"];

export async function runFsck(input: FsckInput): Promise<FsckResult> {
  const timestamp = new Date().toISOString();
  const layers = input.layer ? [input.layer] : ALL_LAYERS;
  let findings: FsckFinding[] = [];
  let lanceState: FsckLanceState = "unchecked";
  let fatalError: string | undefined;
  try {
    if (layers.includes("vault")) findings = findings.concat(probeVault(input.vaultPath, input.db));
    if (layers.includes("sqlite")) findings = findings.concat(probeSqlite(input.db));
    if (layers.includes("fts")) findings = findings.concat(probeFts(input.db));
    if (layers.includes("lance")) {
      const r = await probeLance(input.lancePath, input.db);
      findings = findings.concat(r.findings);
      lanceState = r.state;
    }
  } catch (e) {
    fatalError = e instanceof Error ? `fsck probe failed: ${e.message}` : "fsck probe failed (unknown)";
  }
  const report = buildReport(findings, lanceState, timestamp, fatalError);
  const exitCode: 0 | 1 | 2 = fatalError ? 2 : report.overallStatus === "pass" ? 0 : 1;
  return { report, exitCode };
}

export function register(program: Command): void {
  program
    .command("fsck")
    .description("只读存储一致性检查（vault/SQLite/FTS/LanceDB 四层对齐 + FK 孤儿），不写数据")
    .option("--json", "输出稳定 FsckReport JSON（供下游 Agent 解析）")
    .option("--layer <name>", "只跑指定层：vault|sqlite|fts|lance")
    .action(async (opts: { json?: boolean; layer?: FsckLayer }) => {
      const { loadConfig } = await import("../context.js");
      const { CBrainDB } = await import("../../storage/sqlite.js");
      const config = loadConfig();
      const db = new CBrainDB(config.dbPath, { skipMigrate: true }); // 同 repair-fk：避免迁移阻塞
      try {
        const { report, exitCode } = await runFsck({
          vaultPath: config.vaultPath, lancePath: config.lancePath, db, layer: opts.layer,
        });
        if (opts.json) console.log(JSON.stringify(report));
        else console.log(reportToMarkdown(report));
        process.exit(exitCode);
      } finally {
        db.close();
      }
    });
}
```

- [ ] **Step 4: 注册到 program.ts**

Run: `grep -n "import.*repair-fk\|registerRepair\|repair-fk" src/cli/program.ts` 找注册位置模式。在 program.ts 加：

```typescript
import { register as registerFsck } from "./commands/fsck.js";
// ...在其它 register 调用旁：
registerFsck(program);
```

- [ ] **Step 5: 跑测试确认通过** — Run: `bun test tests/cli/fsck.test.ts` → PASS（3/3）。

- [ ] **Step 6: 手测 CLI** — Run: `bun run dev fsck --json | head` + `bun run dev fsck` → 确认输出合法 + exit code（用 `echo $?`）。

- [ ] **Step 7: Commit**

```bash
git add src/cli/commands/fsck.ts src/cli/program.ts tests/cli/fsck.test.ts
git commit -m "feat(fsck): CLI orchestrator + exit code 0/1/2 + --json/--layer (#262)"
```

---

## Task 8: 只读断言（acceptance：fsck 不写 DB/vault/LanceDB）

**Files:**
- Test: `tests/cli/fsck.readonly.test.ts`

- [ ] **Step 1: 写只读断言测试**

```typescript
// tests/cli/fsck.readonly.test.ts
import { test, expect, afterEach } from "bun:test";
import { mkdtempSync, rmSync, readdirSync, statSync, writeFileSync, mkdirSync, createHash } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { readFile } from "node:fs/promises";
import { CBrainDB } from "../../src/storage/sqlite.js";
import { runFsck } from "../../src/cli/commands/fsck.js";

let dir: string;
afterEach(() => { if (dir) rmSync(dir, { recursive: true, force: true }); });

async function hash(p: string): Promise<string> {
  const buf = await readFile(p);
  return createHash("sha256").update(buf).digest("hex");
}
function dirMtimes(root: string): string {
  return readdirSync(root).map((n) => `${n}:${statSync(join(root, n)).mtimeMs}`).sort().join("|");
}

test("fsck is read-only: DB hash + vault mtimes + lance dir unchanged", async () => {
  dir = mkdtempSync(join(tmpdir(), "cbrain-fsck-ro-"));
  const dbPath = join(dir, "t.sqlite");
  const vaultPath = join(dir, "vault"); mkdirSync(join(vaultPath, "entities"), { recursive: true });
  const lancePath = join(dir, "lance"); mkdirSync(lancePath);
  writeFileSync(join(vaultPath, "entities", "e.md"), "---\nslug: e\n---\nx");

  const db = new CBrainDB(dbPath);
  db.upsertPage({ slug: "e", type: "entity", title: "e", file_path: "entities/e.md", content: "x" } as any);
  const beforeDb = await hash(dbPath);
  const beforeVault = dirMtimes(vaultPath);
  const beforeLance = dirMtimes(lancePath);
  db.close();

  // reopen + fsck
  const db2 = new CBrainDB(dbPath);
  await runFsck({ vaultPath, lancePath, db: db2 });
  db2.close();

  expect(await hash(dbPath)).toBe(beforeDb);
  expect(dirMtimes(vaultPath)).toBe(beforeVault);
  expect(dirMtimes(lancePath)).toBe(beforeLance);
});
```

- [ ] **Step 2: 跑测试** — Run: `bun test tests/cli/fsck.readonly.test.ts` → 应 PASS（fsck 全程只读）。若 FAIL，定位哪个 probe 误写。

- [ ] **Step 3: Commit**

```bash
git add tests/cli/fsck.readonly.test.ts
git commit -m "test(fsck): assert read-only (DB hash + vault/lance mtimes unchanged) (#262)"
```

---

## Task 9: 收尾（gate + docs 一致性）

- [ ] **Step 1: 全量门禁**

Run: `bun run check`
Expected: PASS（lint + 全量 test）。若 biome 报 lint，按提示修。

- [ ] **Step 2: check:docs 命令注册校验**

`bin/check-docs-consistency.ts` 的 `checkCommands` 会扫 docs/ 里 `cbrain <cmd>`——fsck 是新命令，若 docs/ 未提 fsck 则无校验；若在 README/docs 提及 `cbrain fsck`，gate 会校验它存在（已注册，通过）。
Run: `bun run check:docs` → PASS。

- [ ] **Step 3: README/docs 更新（匿名示例）**

在 `docs/install-onboarding.md` 维护段或 `README.md` CLI 列表加一行 `cbrain fsck`（保持匿名，不带真 slug）。若加了 `cbrain fsck` 字样，check:docs 会校验命令存在（已注册，过）。

- [ ] **Step 4: CHANGELOG 加一行**

`CHANGELOG.md` 顶部（Unreleased 或当前版本段）加：
```
- feat(ops): `cbrain fsck` 只读存储一致性检查（vault/SQLite/FTS/LanceDB 四层 + FK 孤儿），exit 0/1/2，--json 稳定 schema（#262）
```

- [ ] **Step 5: 最终全量验证**

Run: `bun run check && bun run check:docs`
Expected: 全 PASS。

- [ ] **Step 6: Commit**

```bash
git add CHANGELOG.md README.md docs/install-onboarding.md
git commit -m "docs(fsck): changelog + cli mention (#262)"
```

---

## Self-Review

**Spec coverage**（spec 每条 → task）：
- 检查项 1/2/3（vault 对齐）→ Task 6 ✓
- 检查项 4/5/8/9/10/11（sqlite）→ Task 3 ✓
- 检查项 6（FTS）→ Task 4 ✓
- 检查项 7（LanceDB）→ Task 5 ✓
- JSON schema（FsckReport/findings）→ Task 1 ✓
- severity→overallStatus→exit code → Task 1(types) + Task 2(report) + Task 7(cli) ✓
- LanceDB 不建表/目录 → Task 5（existsSync 预检 + openChunksStrict + 测试断言）✓
- 匿名化 → Task 2(anonymizeSlugs) + 各 probe 用 ✓
- 只读保证 → Task 8（前后 hash/mtime 断言）✓
- `--json` / `--layer` → Task 7 ✓
- 无 LLM / 不执行 suggestedCommand / 无 schema 迁移 → 全程未引入 ✓

**Placeholder scan**：测试里 `as any` 构造 PageRow + `addChunk?.` 是临时 stub，Task 3/4 Step 3 都有「grep 确认真实签名后替换」的显式步骤，非遗留 placeholder。

**Type consistency**：`FsckFinding` / `FsckReport` / `FsckLanceState` 在 Task 1 定义，Task 2-7 全部沿用同名字段（`check/layer/severity/count/sampleSlugs/detail/suggestedCommand`）；`runFsck` 在 Task 7 定义、Task 8 复用；`probeSqlite/probeFts/probeLance/probeVault` 签名一致。已核对。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-07-01-fsck.md`. 实现前先开 L 级 worktree（宏哥指示：plan 定了再开）。两种执行方式：

**1. Subagent-Driven（推荐）** — 每个 task 派 fresh subagent，task 间 review，迭代快。fsck 的 9 个 task 边界清晰、互相独立（types→report→4 probes→cli→readonly→收尾），适合逐 task 派发。

**2. Inline Execution** — 本会话内按 executing-plans 批量执行 + checkpoint review。
